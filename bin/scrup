#!/bin/sh

saveloc=$HOME/Pictures/Screenshots/`date +%F_%H%M%S_%N`.jpg
area="-g 1920x1080+0+0"
crop=""


maim $crop $area $saveloc -c 1,0,0.5,0.2 -l &&
notify-send -a "scrup" "uploading" &&
imgurbash2 $saveloc &&
notify-send -a "scrup" "url copied"

exit

# credits: github/uoou
