# vim: ft=conf

keybind = clear
{{- if eq .chezmoi.os "darwin" }}
macos-option-as-alt = true
window-decoration = true
macos-titlebar-style = hidden
macos-non-native-fullscreen = true
keybind = ctrl+shift+enter=toggle_quick_terminal
{{- else }}
  {{- if eq .chezmoi.hostname "asahi" }}
window-height = 30
window-width = 90
  {{- else }}
window-height = 44
window-width = 130
  {{- end }}
resize-overlay = never
{{- if ne (env "XDG_CURRENT_DESKTOP") "KDE" }}
window-decoration = false
{{- else }}
gtk-titlebar = false
{{- end }}
window-theme = system
{{- end }}

config-file = theme
term = xterm-256color

config-file = fonts
font-size = 14

window-padding-balance = false
window-padding-x = 17,0
window-padding-y = 4,0

cursor-style = block
cursor-style-blink = false
cursor-invert-fg-bg = true

mouse-hide-while-typing = true
mouse-scroll-multiplier = 0.5
mouse-shift-capture = true
copy-on-select = clipboard

shell-integration = fish

# keybinds
keybind = ctrl+comma=open_config
keybind = ctrl+shift+n=new_window
keybind = ctrl+shift+comma=reload_config

keybind = shift+page_up=scroll_page_up
keybind = shift+page_down=scroll_page_down
keybind = shift+end=scroll_to_bottom

keybind = ctrl+shift+c=copy_to_clipboard
keybind = ctrl+shift+v=paste_from_clipboard
keybind = shift+insert=paste_from_selection

keybind = ctrl+8=decrease_font_size:1
keybind = ctrl+9=increase_font_size:1
keybind = ctrl+equal=increase_font_size:1
keybind = ctrl+minus=decrease_font_size:1
keybind = ctrl+zero=reset_font_size

keybind = ctrl+shift+page_down=jump_to_prompt:1
keybind = ctrl+shift+page_up=jump_to_prompt:-1

